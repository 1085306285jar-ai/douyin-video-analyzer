# 安全与隐私

## 网络请求

运行时仅允许为用户提交的任务访问抖音网页、短链跳转地址和抖音使用的媒体 CDN。不得加入遥测、崩溃上报、广告 SDK、在线更新器或第三方 AI 请求。

## 本地敏感数据

- 不在日志中打印完整 URL、Cookie、媒体内容或转写文本。
- 兼容浏览器使用独立配置目录，不读取个人浏览器配置。
- 导出的 Netscape Cookie 文件必须只包含 `douyin.com`/`iesdouyin.com`，任务结束删除。
- 临时媒体必须位于应用专属临时目录，逐条及任务结束双重清理。

## 权限

程序不要求管理员权限。若 EXE 所在目录不可写，输出应回退到当前用户本地数据目录，不得请求 UAC 提权。

## 内容边界

仅支持公开内容。不得加入破解验证码、伪造账号身份、绕过私密/付费内容、批量撞库或窃取浏览器 Cookie 的实现。
