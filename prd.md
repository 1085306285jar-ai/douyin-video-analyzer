# 抖音视频 AI 智能解析工具 PRD（执行版）

版本：1.0
目标平台：Windows 10/11 x64
交付：`抖音视频AI解析工具.exe`

## 产品目标

为短视频运营人员提供一个低门槛桌面工具：从用户主动提交的抖音公开链接读取媒体，在本机生成可检索、可复盘、可二次编辑的口播文本与结构化报告。

## 主流程

1. 用户粘贴分享文案或 URL。
2. 程序校验域名并识别单视频、合集、博主主页。
3. 常规提取器读取公开条目；受风控限制时按用户选择启动可见 Edge 兼容窗口。
4. 逐条过滤时长不超过 10 秒的内容。
5. 下载最合适的音频或带音轨媒体到任务临时目录。
6. Faster-Whisper 在 CPU 上离线转写。
7. 本地分析器生成固定五模块报告。
8. 自动导出 TXT、Markdown；批量任务生成索引。
9. 删除临时媒体和任务 Cookie。

## 固定分析结构

1. 视频核心主题：一句话，不照抄开头 60 字。
2. 核心观点：2—5 条，来自原文且做相似观点去重。
3. 高光金句：1—3 条，只能逐字选自原文，不生成伪引语。
4. 内容情绪属性：产品测评、带货推荐、吐槽评论、情感鸡汤、干货/科普、日常分享之一。
5. 推荐话题标签：优先原视频标签与正文关键词，共 5 个。

## 批量规则

- 默认上限 20，范围 1—100。
- 逐条独立状态：成功、跳过、失败。
- 私密、登录限制、失效、无口播、过短属于跳过；网络/组件/未知错误属于失败。
- 单条失败后继续下一条。
- 用户可取消，当前可中断点结束后立即停止并清理。

## 数据与隐私

- 不上传链接、媒体、字幕、报告。
- 不调用第三方解析服务、在线 AI 或付费 API。
- 不在日志中写链接或原文。
- 独立 Edge 的持久登录状态仅存本机；任务 Cookie 文件任务结束删除。
- 输出默认放 EXE 同级 `output`；目录不可写时使用当前用户本地数据目录。

## 质量要求

- UI 工作线程与耗时线程隔离，下载/转写期间窗口可移动、可缩放、可取消。
- 所有用户可见错误使用中文描述，不暴露堆栈和敏感请求细节。
- 输出 UTF-8，Windows 记事本直接打开不乱码。
- 同名文件自动增加序号，不覆盖历史结果。
- 单文件启动立即显示闪屏；主窗口启动后关闭闪屏。
- 打包后提供内部 `--self-test`，构建流程必须执行并通过。
