# 需求审定记录

## 一句话产品定义

粘贴抖音公开链接，程序尽可能稳定地读取媒体，并在 Windows 本机完成中文转写、结构化内容拆解和文件导出；最终以无需安装依赖的单 EXE 交付。

## 原需求中需要纠正的地方

| 原说法 | 审定后的可执行口径 |
| --- | --- |
| 输入互联网链接但“全程无联网” | 读取抖音页面必须联网；转写、分析、存储全本地，不连接云端 AI 或第三方解析 API。 |
| 任意公开链接必定成功 | 抖音风控是外部变量；常规提取失败时使用可见 Edge 兼容窗口，仍无法读取则明确告知原因，绝不伪造成功。 |
| 无需登录且主页/合集全量抓取 | 普通公开内容优先免登录；平台要求验证时允许用户在独立 Edge 中手动验证。默认批量上限 20，最高 100，防止误操作。 |
| 关键词模板就是“AI 分析” | 改为本地句子图排序、观点去重、原文金句筛选、类型评分和关键词提取；不把整篇原文冒充金句。 |
| 所有单视频 10 秒内完成 | 解析时间与视频长度、网络、CPU 有关；验收改为进度可见、界面不冻结、可取消并显示实际耗时。 |
| Whisper + FFmpeg + PyTorch 全塞进 EXE | 使用 Faster-Whisper/CTranslate2 CPU int8；PyAV 已携带 FFmpeg 解码库，不额外带一个外部 `ffmpeg.exe`。 |
| 大模型单文件双击立即见主界面 | 单文件启动需解压；加入 bootloader 闪屏，用户双击后立即得到反馈。 |
| “程序永不出错” | 所有已知异常友好捕获、逐条隔离、不中断批量；未预期错误安全停止并清理临时文件。 |

## V1.0 功能边界

### 必须完成

- Windows 10/11 x64 单 EXE、无控制台黑窗、无需安装依赖。
- 分享文本/短链/直链识别，域名白名单防止误处理其他站点。
- 单视频、合集、博主主页；主页取最新 N 条，N 可配置 1—100。
- 常规提取 + 可选可见 Edge 兼容提取两层策略。
- 本地 Whisper 中文转写、VAD 静音过滤、CPU int8。
- 主题、2—5 条核心观点、1—3 条原文金句、内容属性、5 个标签。
- TXT、Markdown、批量索引；按日期归档；同名文件不覆盖。
- 实时日志、进度条、任务取消、列表结果、详情切换、打开输出目录。
- 私密/登录限制/失效/无语音/短视频/网络异常分类提示。
- 临时媒体和临时 Cookie 清理，内容不写运行日志。

### 明确不做

- 不破解验证码、不伪造签名、不绕过私密/付费权限。
- 不调用联网大模型或云端语音 API。
- 不承诺平台规则变化后永久零维护。
- 不下载图集用于 OCR；V1 只分析有可转写音轨的视频作品。
- 不做说话人分离、逐字校对、字幕编辑器或二次创作生成。

## 验收定义

“通过”必须同时满足：

1. 打包后自检通过：依赖、模型、写入权限完整。
2. 全部自动化测试通过。
3. 干净 Win10/Win11 双击均先出现闪屏、随后出现 GUI，无黑窗。
4. 真实公开单视频完成抓取、转写、分析、TXT/MD 导出。
5. 主页/合集常规或兼容模式至少一种路径可完成批量处理。
6. 任一单条失败不拖垮后续队列，取消任务后临时文件被清理。
7. 断网监测证明本地转写与分析不产生第三方 AI 请求；联网请求只面向抖音页面/CDN。

## 当前实现状态

- 核心代码、GUI、双层提取、离线分析、导出、打包脚本和自动化测试已经建立。
- 当前工作环境为 Linux，因此已完成跨平台核心测试，但不能冒充已完成 Windows 原生 EXE 的双击验收。
- 下一硬门槛是在 Windows x64 构建机运行 `build_windows.bat`，再用真实抖音链接执行发布前人工矩阵。
